package game

// Trade balance constants (game layer only — not UI).
const (
	TradeCreditsGain = 40
	TradeFoodCost    = 5
	TradeMoraleCost  = 3
	// MinFoodToTrade: trade allowed only when Food is strictly greater than this value.
	MinFoodToTrade = 30
)
